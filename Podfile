project 'AppAuthWebView-ios.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppAuthWebView-ios' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppAuthWebView-ios

  pod 'AppAuth'

  target 'AppAuthWebView-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppAuthWebView-iosUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
